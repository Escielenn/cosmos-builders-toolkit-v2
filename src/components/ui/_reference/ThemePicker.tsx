/**
 * StellarForge — ThemePicker (reference)
 *
 * The live implementation is src/components/settings/AppearanceSettings.tsx
 * with src/hooks/use-theme.ts. This file exists so the pattern is visible
 * next to the other reference components; do not import it from app code.
 */
export { default as ThemePicker } from "@/components/settings/AppearanceSettings";
export { useTheme, initTheme } from "@/hooks/use-theme";
