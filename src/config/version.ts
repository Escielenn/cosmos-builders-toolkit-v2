// StellarForge version, pre-1.0, four-digit build number.
// Convention (from git history): +1 for a fix, +10 for feature-scale work.
// Bump in the same commit as the change and name the version in the subject.
export const APP_VERSION = "0.7030"
